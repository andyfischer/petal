// if / elsif / else as an expression — no values are mutated.
fn classify(n)
  if n < 0 then "negative"
  elsif n == 0 then "zero"
  else "positive"
  end
end
print(classify(-3))
print(classify(0))
print(classify(7))
