// Mutating record fields in a loop: each assignment copy-on-writes the map.
let acc = { sum: 0, count: 0 }
for x in [10, 20, 30] do
  acc.sum = acc.sum + x
  acc.count = acc.count + 1
end
print(acc.sum)
print(acc.count)
