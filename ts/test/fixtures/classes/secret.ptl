// A module with a class it does not export. `export` governs the class name
// in *both* positions: an importer may neither call `Secret(...)` nor write
// `Secret` in a type annotation.

class Secret
  a: int
end

export fn make(n: int) -> Secret
  Secret(n)
end

export fn peek(s: Secret) -> int
  s.a
end
