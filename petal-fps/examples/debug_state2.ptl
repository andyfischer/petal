state a = 0
state b = 0
state c = 0

// unconditional
a = a + 1

// conditional — always true
if true then
    b = b + 1
end

// conditional on input
if key_down("w") then
    c = c + 1
end

log("a=" ++ str(a) ++ " b=" ++ str(b) ++ " c=" ++ str(c))

sky_gradient(0, 0, 0, 0, 0, 0)
