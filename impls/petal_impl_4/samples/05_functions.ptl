// Functions demo (simplified - user-defined functions not fully implemented)
// Built-in functions work:

print("Built-in functions:")
print("sqrt(16) =", sqrt(16))
print("len([1, 2, 3]) =", len([1, 2, 3]))
print("len(\"hello\") =", len("hello"))

// Arrow functions are parsed but simplified
fn add(a, b) -> a + b

// Function calls use builtins for now
let result = sqrt(144)
print("sqrt(144) =", result)
